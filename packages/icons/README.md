# @koi-ui/icons

Vendored Heroicons React components for Koi UI.

```tsx
import { XCircleIcon } from '@koi-ui/icons/24/outline';
```

The icon source SVGs are copied from Heroicons v2.2.0 and transformed into local TSX components. See `LICENSE.heroicons` for upstream attribution.
