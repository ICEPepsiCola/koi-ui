# AutoComplete

## Basic usage

```tsx preview
import { AutoComplete } from '@koi-ui/core';
export default () => <AutoComplete options={[{ value:'apple', label:'Apple' }, { value:'banana', label:'Banana' }]} placeholder="Search" />;
```

## clearable

```tsx preview
import { AutoComplete } from '@koi-ui/core';
import { useState } from 'react';
export default () => {
  const [value, setValue] = useState('apple');
  return <AutoComplete value={value} onChange={setValue} clearable options={[{ value:'apple', label:'Apple' }, { value:'banana', label:'Banana' }]} placeholder="Search" />;
};
```

## API

### AutoComplete

| Property | Description | Type | Default |
| :---: | :---: | :---: | :---: |
| options | Options | `AutoCompleteOption[]` **(required)** | `-` |
| className | Class name | `string \|​ undefined` | `-` |
| clearable | - | `boolean \|​ undefined` | `false` |
| defaultValue | Default value | `string \|​ undefined` | `` |
| disabled | Disabled | `boolean \|​ undefined` | `false` |
| error | Error message | `string \|​ boolean \|​ undefined` | `-` |
| filterOption | - | `((input: string,​ option: AutoCompleteOption) => boolean) \|​ undefined` | `-` |
| onChange | Change callback | `((value: string) => void) \|​ undefined` | `-` |
| onSelect | Select callback | `((option: AutoCompleteOption) => void) \|​ undefined` | `-` |
| placeholder | Placeholder | `string \|​ undefined` | `Enter text` |
| size | Size | `enum` | `md` |
| value | Value | `string \|​ undefined` | `-` |
