import { ApiDocs } from '../../ApiDocs';

# Icon

## Basic usage

```tsx preview
import { Icon, Space } from '@koi-ui/core';
export default () => (
  <Space gap={4}>
    <Icon name="search" />
    <Icon name="check" />
    <Icon name="close" />
  </Space>
);
```

## Common icons

```tsx preview
import { Icon, Space } from '@koi-ui/core';
export default () => (
  <Space gap={6}>
    <Icon name="home" />
    <Icon name="user" />
    <Icon name="settings" />
    <Icon name="bell" />
  </Space>
);
```

## API

<ApiDocs name="Icon" />
