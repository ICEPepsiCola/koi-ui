# Radio

## Basic usage

```tsx preview
import { Radio, RadioGroup } from '@koi-ui/core';
export default () => (
  <RadioGroup defaultValue="a">
    <Radio radioValue="a" label="Option A" />
    <Radio radioValue="b" label="Option B" />
  </RadioGroup>
);
```

## Controlled radio group

```tsx preview
import { Radio, RadioGroup, Text } from '@koi-ui/core';
import { useState } from 'react';
export default () => {
  const [value, setValue] = useState('a');
  return (
    <div>
      <RadioGroup value={value} onChange={setValue}>
        <Radio radioValue="a" label="Plan A" />
        <Radio radioValue="b" label="Plan B" />
      </RadioGroup>
      <Text muted className="mt-2 block text-sm">Current: {value}</Text>
    </div>
  );
};
```

## Size, direction & disabled

```tsx preview
import { Radio, RadioGroup, Space, Text } from '@koi-ui/core';
export default () => (
  <Space direction="vertical" size="middle" className="w-full">
    <div>
      <Text muted size="sm" className="mb-2 block">size</Text>
      <Space>
        <RadioGroup size="sm" defaultValue="sm" direction="horizontal">
          <Radio radioValue="sm" label="sm" />
        </RadioGroup>
        <RadioGroup size="md" defaultValue="md" direction="horizontal">
          <Radio radioValue="md" label="md" />
        </RadioGroup>
        <RadioGroup size="lg" defaultValue="lg" direction="horizontal">
          <Radio radioValue="lg" label="lg" />
        </RadioGroup>
      </Space>
    </div>
    <div>
      <Text muted size="sm" className="mb-2 block">direction=&quot;horizontal&quot;</Text>
      <RadioGroup defaultValue="a" direction="horizontal">
        <Radio radioValue="a" label="Option A" />
        <Radio radioValue="b" label="Option B" />
        <Radio radioValue="c" label="Option C" />
      </RadioGroup>
    </div>
    <RadioGroup disabled defaultValue="a">
      <Radio radioValue="a" label="Group disabled" />
      <Radio radioValue="b" label="Unavailable" />
    </RadioGroup>
  </Space>
);
```

## Colors

```tsx preview
import { Radio, RadioGroup, Space } from '@koi-ui/core';
export default () => (
  <Space direction="vertical" size="middle" className="w-full">
    <RadioGroup color="primary" defaultValue="a" direction="horizontal">
      <Radio radioValue="a" label="primary" />
      <Radio radioValue="b" label="Off" />
    </RadioGroup>
    <RadioGroup color="success" defaultValue="a" direction="horizontal">
      <Radio radioValue="a" label="success" />
      <Radio radioValue="b" label="Off" />
    </RadioGroup>
    <RadioGroup color="error" defaultValue="a" direction="horizontal">
      <Radio radioValue="a" label="error" />
      <Radio radioValue="b" label="Off" />
    </RadioGroup>
  </Space>
);
```

## API

<API name="Radio" />
