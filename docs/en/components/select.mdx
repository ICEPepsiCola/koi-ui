# Select

## Basic usage

```tsx preview
import { Select } from '@koi-ui/core';
import { useState } from 'react';
export default () => {
  const [v, setV] = useState('');
  return (
    <Select
      options={[
        { label: 'Beijing', value: 'bj' },
        { label: 'Shanghai', value: 'sh' },
      ]}
      value={v}
      onChange={setV}
      placeholder="Select city"
    />
  );
};
```

## Controlled select

```tsx preview
import { Select, Text } from '@koi-ui/core';
import { useState } from 'react';
export default () => {
  const [city, setCity] = useState('sh');
  return (
    <div>
      <Select
        value={city}
        onChange={setCity}
        placeholder="Select city"
        options={[
          { label: 'Beijing', value: 'bj' },
          { label: 'Shanghai', value: 'sh' },
          { label: 'Guangzhou', value: 'gz' },
        ]}
      />
      <Text muted className="mt-2 block text-sm">Current: {city}</Text>
    </div>
  );
};
```

## clearable

```tsx preview
import { Select } from '@koi-ui/core';
import { useState } from 'react';
export default () => {
  const [city, setCity] = useState('sh');
  return (
    <Select
      value={city}
      onChange={setCity}
      clearable
      placeholder="Select city"
      options={[
        { label: 'Beijing', value: 'bj' },
        { label: 'Shanghai', value: 'sh' },
        { label: 'Guangzhou', value: 'gz' },
      ]}
    />
  );
};
```

## Size

```tsx preview
import { Select, Space } from '@koi-ui/core';
const options = [
  { label: 'Beijing', value: 'bj' },
  { label: 'Shanghai', value: 'sh' },
];
export default () => (
  <Space direction="vertical" size="middle" className="w-full">
    <Select size="sm" options={options} placeholder="sm" />
    <Select size="md" options={options} placeholder="md" />
    <Select size="lg" options={options} placeholder="lg" />
  </Space>
);
```

## Disabled

```tsx preview
import { Select } from '@koi-ui/core';
export default () => (
  <Select
    disabled
    value="sh"
    options={[
      { label: 'Beijing', value: 'bj' },
      { label: 'Shanghai', value: 'sh' },
    ]}
    placeholder="Select city"
  />
);
```

## API

<API name="Select" />
