
# Segmented

## Basic usage

```tsx preview
import { Segmented } from '@koi-ui/core';
import { useState } from 'react';
export default () => { const [v,setV]=useState('a'); return <Segmented value={v} onChange={setV} options={[{label:'List',value:'a'},{label:'Card',value:'b'}]} />; };
```

## API

<API name="Segmented" />
