
# Toast 轻提示

## 基础用法

```tsx preview
import { Button, toast } from '@koi-ui/core';
export default () => <Button onClick={() => toast.success('操作成功')}>显示提示</Button>;
```

## API

<API name="Toast" />
