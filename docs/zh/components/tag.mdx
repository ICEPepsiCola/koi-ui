# Tag 标签

## 基础用法

```tsx preview
import { Tag, Space } from '@koi-ui/core';
export default () => (
  <Space wrap>
    <Tag>默认</Tag>
    <Tag variant="soft">Soft</Tag>
    <Tag variant="outline">Outline</Tag>
    <Tag variant="ghost">Ghost</Tag>
    <Tag variant="success">成功</Tag>
    <Tag variant="warning">警告</Tag>
    <Tag variant="destructive">危险</Tag>
  </Space>
);
```

## 可关闭标签

```tsx preview
import { Tag, Space } from '@koi-ui/core';
import { useState } from 'react';
export default () => {
  const [tags, setTags] = useState(['React', 'TypeScript', 'Tailwind']);
  return (
    <Space wrap>
      {tags.map((tag) => (
        <Tag key={tag} closable onClose={() => setTags(tags.filter((t) => t !== tag))}>{tag}</Tag>
      ))}
    </Space>
  );
};
```

## API

### Tag

| 属性 | 描述 | 类型 | 默认值 |
| :---: | :---: | :---: | :---: |
| closable | 是否可关闭 | `boolean \|​ undefined` | `-` |
| onClose | 关闭回调 | `(() => void) \|​ undefined` | `-` |
| variant | 变体 | `enum` | `-` |
