import { ApiDocs } from '../../ApiDocs';

# NavBar 导航栏

## 基础用法

```tsx preview
import { NavBar } from '@koi-ui/core';
export default () => <NavBar title="页面标题" onBack={() => {}} />;
```

## 带右侧操作

```tsx preview
import { NavBar, Button } from '@koi-ui/core';
export default () => (
  <NavBar
    title="详情页"
    onBack={() => {}}
    right={<Button size="sm" variant="ghost">分享</Button>}
  />
);
```

## API

<ApiDocs name="NavBar" />
