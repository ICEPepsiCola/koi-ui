import { ApiDocs } from '../../ApiDocs';

# Timeline 时间轴

## 基础用法

```tsx preview
import { Timeline } from '@koi-ui/core';
export default () => <Timeline items={[{ key:'1', children:'创建项目' }, { key:'2', children:'上线发布' }, { key:'3', children:'持续迭代' }]} />;
```

## API

<ApiDocs name="Timeline" />
